/** values should be same as file names from templates folder */
export enum EMailTemplatesType {
  ACTIVATION = 'activation',
  RESTORE_PASSWORD = 'restore-password',
  NEW_EMAIL_CONFIRMATION = 'new-email-confirmation',
}
